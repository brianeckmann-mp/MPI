{
  "name": "varsity-signal-figma-plugin",
  "version": "1.0.0",
  "private": true,
  "description": "Figma plugin that generates the Friday Night Fever Dream / Varsity Signal MaxPreps style guide.",
  "scripts": {
    "build": "esbuild code.ts --bundle --outfile=dist/code.js --target=es2017",
    "watch": "esbuild code.ts --bundle --outfile=dist/code.js --target=es2017 --watch"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.110.0",
    "esbuild": "^0.25.0",
    "typescript": "^5.8.0"
  }
}
